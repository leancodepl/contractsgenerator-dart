name: example
description: A simple command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  leancode_contracts:
    path: ../../leancode_contracts
  leancode_contracts_generator:
    path: ..
dev_dependencies:
  leancode_lint: ^6.0.0
